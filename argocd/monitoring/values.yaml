grafana:
  adminPassword: admin123

  service:
    type: ClusterIP

prometheus:
  service:
    type: ClusterIP

alertmanager:
  enabled: true

defaultRules:
  create: true

kubeStateMetrics:
  enabled: true

nodeExporter:
  enabled: true